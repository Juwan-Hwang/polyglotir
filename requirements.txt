# SILP — Python dependencies (Windows / CPU-only)
# Install torch first with CPU-only index:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Then:
#   pip install -r requirements.txt

# ── Core ──────────────────────────────────────────
pydantic>=2.5.0
click>=8.1.7
rich>=13.7.0

# ── ML (local models + tokenizers) ────────────────
torch>=2.1.0
transformers>=4.36.0
tiktoken>=0.5.0

# ── API clients (closed-source models) ───────────
openai>=1.6.0
anthropic>=0.18.0
google-generativeai>=0.3.2

# ── Analysis ──────────────────────────────────────
pandas>=2.1.0
scipy>=1.11.0
matplotlib>=3.7.0
seaborn>=0.13.0

# ── Dev ───────────────────────────────────────────
pytest>=7.4.0
ruff>=0.1.0
